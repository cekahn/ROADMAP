{
	"$schema": "http://json-schema.org/draft-07/schema#",
	"$id": "ROADMAP.partition.json",
	"description": "Description of each partition",
	"type": "object",
	"properties": {
		"Subsets": {
			"description": "Subsets of partition by age, sex, and/or race.",
			"type": "array",
			"items": {
				"$ref": "ROADMAP.subset.json#"
			}
		},
		"Comments": {
			"$comment": "ROADMAP:00070",
			"type": "string"
		}
	},
	"additionalProperties": false
}